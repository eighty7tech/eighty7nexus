import { BrandFormSkeleton } from "@/components/admin/brand-form-skeleton";

// Shadows admin/brands/loading.tsx — see the sibling under new/.
export default function AdminBrandEditLoading() {
  return <BrandFormSkeleton />;
}
