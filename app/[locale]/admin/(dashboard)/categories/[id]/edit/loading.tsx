import { CategoryFormSkeleton } from "@/components/admin/category-form-skeleton";

// Shadows admin/categories/loading.tsx — see the sibling under new/.
export default function AdminCategoryEditLoading() {
  return <CategoryFormSkeleton />;
}
