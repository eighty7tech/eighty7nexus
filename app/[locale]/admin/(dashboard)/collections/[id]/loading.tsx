import { CollectionFormSkeleton } from "@/components/admin/collection-form-skeleton";

// Shadows admin/collections/loading.tsx — see the sibling under new/.
export default function AdminCollectionEditLoading() {
  return <CollectionFormSkeleton />;
}
